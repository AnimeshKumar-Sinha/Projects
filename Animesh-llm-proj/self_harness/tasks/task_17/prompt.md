Write a Python function called `solution(s)` that returns the most frequent character in string `s`. If there is a tie, return the one that appears first in the string.

Example: `solution("aabbc")` → `"a"`
