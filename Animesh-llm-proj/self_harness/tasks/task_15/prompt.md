Write a Python function called `solution(lst)` that rotates the list `lst` one position to the right. The last element wraps around to become the first.

Example: `solution([1, 2, 3, 4])` → `[4, 1, 2, 3]`
