Write a Python function called `solution(s)` that returns `True` if `s` is a palindrome (reads the same forwards and backwards), `False` otherwise.

Example: `solution("racecar")` → `True`, `solution("hello")` → `False`
