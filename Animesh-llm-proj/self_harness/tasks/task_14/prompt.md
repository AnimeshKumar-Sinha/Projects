Write a Python function called `solution(s)` that returns the string `s` with all duplicate characters removed, keeping only the first occurrence of each character.

Example: `solution("aabbcc")` → `"abc"`, `solution("abcabc")` → `"abc"`
