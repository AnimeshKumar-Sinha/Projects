Write a Python function called `solution(lst)` that returns the two numbers from list `lst` that add up to zero. Return them as a sorted list. Assume exactly one such pair exists.

Example: `solution([1, -1, 2, 3])` → `[-1, 1]`
